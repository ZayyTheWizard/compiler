func main() {
    let a = "My Name is Isaiah"
    let b = 4 + 20 + (6 * 9)
    
    if (1 < 2) {
        return 1;
    }

    for i = 0; i < c.size(); i+=1 {
        b+=4
    }

    while(true) {
        return false;
    }
    
    hello_world()
    return b;
}